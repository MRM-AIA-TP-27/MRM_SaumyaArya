# Task 3

Downloaded a CSV file from WhatsApp and moved it to the `GitHub_task` folder.
Added it to Git, committed and pushed it to the GitHub repository.

## Commands Used

```sh
cd GitHub_task
mv /mnt/c/Users/aarya/Downloads/data.csv 
git add data.csv
git commit -m "Task 3: Added CSV file"
git push
```
